import { api } from 'utils/helper';

export const get{{pascalCase name}} = (payload) => {
  return api
    .get({
      url: '{{pascalCase name}}.do',
      data: payload,
    })
    .then((response) => response.data);
};
