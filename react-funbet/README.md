## How to use docker for nginx.gitignore

- Run `docker-compose up -d` to start up base containers for the first time starting up.
- Run `docker-compose stop` to stop containers.
- See logs of nginx in `./logs` folder
