'use strict';

exports.__esModule = true;
exports.default = ensureDeprecatedOptions;

var _deprecate = require('../../warnings/deprecate');

var _deprecate2 = _interopRequireDefault(_deprecate);

var _getCallsite = require('../../errors/get-callsite');

var _getCallsite2 = _interopRequireDefault(_getCallsite);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

function ensureDeprecatedOptions(callsiteName, options) {
    if (options && (options.text || options.index || options.dependencies)) {
        var callsite = (0, _getCallsite2.default)(callsiteName);

        if (options.text) {
            (0, _deprecate2.default)(callsite, {
                what: 'selectorOptions.text',
                useInstead: 'selector.withText()'
            });
        }

        if (options.index) {
            (0, _deprecate2.default)(callsite, {
                what: 'selectorOptions.index',
                useInstead: 'selector.nth()'
            });
        }

        if (options.dependencies) {
            (0, _deprecate2.default)(callsite, {
                what: 'selectorOptions.dependencies',
                useInstead: 'hierarchical selectors (e.g. selector.find())'
            });
        }
    }
}
module.exports = exports['default'];